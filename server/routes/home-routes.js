const router = require('express').Router();
const { 
    // insert variables here from CONTROLLERS
} = require('../../controllers/home');

// UPDATE ACCORDINGLY; PLACED EXAMPLE ROUTES FOR NOW
// // set up POST /home
// router
//     .route('/')
//     .get(getAllUsers)
//     .post(createUser);

module.exports = router;